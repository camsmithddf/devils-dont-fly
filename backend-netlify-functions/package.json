{
  "name": "devils-dont-fly-backend",
  "version": "1.0.0",
  "description": "Netlify functions for Devils Don't Fly payment integration (Stripe + PayPal)",
  "main": "index.js",
  "scripts": {
    "start": "netlify-lambda serve functions",
    "build": "netlify-lambda build functions"
  },
  "dependencies": {
    "stripe": "^12.0.0",
    "@paypal/checkout-server-sdk": "^1.0.3"
  }
}